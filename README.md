# underestimating_hyperplanes

This package provides utilities to compute tangential planes of a multivariate function and filter intersecting hyperplanes using `autograd`.

## Install

```bash
pip install .
